version: "3.9"

services:
  model-plunger:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: model-plunger-app
    ports:
      - "5173:5173"
      - "8080:8080"
    volumes:
      - ./:/app
      - /app/node_modules
    env_file:
      - .env
